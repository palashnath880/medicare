import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: '#008037', // your primary color
        },
        secondary: {
            main: '#faeaeb', // your secondary color
        },
    },
    typography: {
        fontFamily: ``
    }
});

export default theme;